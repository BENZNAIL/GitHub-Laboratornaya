# API для сбора данных  
## Эндпоинты  
- `GET /data` — возвращает JSON с исследовательскими данными.  
Пример запроса:  
```python
import requests  
response = requests.get("https://api.example.com/data") 